name: Actions

on:
  push:
    branches: -main

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: https://ftp.guborges.com.br/
          username: gustavo@guborges.com.br
          password: Gu80rg3$-infoact
          server-dir: /home/guborges/domains/guborges.com.br/public_html/
